import RutileContext from '../../../models/RutileContext';
import { memoryGet } from '../utils/memory';

export function storageStore(context: RutileContext, pathOffset: number, valueOffset: number) {
}
