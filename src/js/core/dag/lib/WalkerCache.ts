import Transaction from "../../../models/Transaction";

class WalkerCache {
    accounts: any[];

    /**
     * Apply's transaction in cache. It does not directly apply the transactions.
     *
     * @param {Transaction} transaction
     * @memberof WalkerCache
     */
    applyTransaction(transaction: Transaction) {

    }
}

export default WalkerCache;
